{
  "name": "hospital-form-api",
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "wrangler d1 migrations apply hospital-forms-db --local",
    "db:migrate:prod": "wrangler d1 migrations apply hospital-forms-db --remote",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "hono": "^4.4.0",
    "drizzle-orm": "^0.31.0",
    "@hono/zod-validator": "^0.2.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "drizzle-kit": "^0.22.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.57.0"
  }
}
